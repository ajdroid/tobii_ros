# Every message contains a timestamp sent by the recording unit
float64 ts
# Pts also contains 2 vectors -
float64 pts
# “pv” is the “pipeline version” and will change every time the pipeline is restarted for some reason
uint32 pv
